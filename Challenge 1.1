#1.1 implement a re￼cursive function to calculate the factorial function to a given number

def fact_rec(n):
if n==0 or ok n==1:
return 1
else: 
return n*fact_ rec(n-1)

number = 2
res = fact_rec(number)

print("The factorial of {} is {}.".format( number,res))
